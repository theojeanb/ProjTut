{#https://getbootstrap.com/docs/4.0/components/navbar/#}


<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('index_index')}}">Projet Tuteuré <span style="color:#26ff1e;">  Menu User </span></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="DropdownInventaire" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Inventaire
                </a>
                <div class="dropdown-menu" aria-labelledby="DropdownInventaire">
                    <a class="dropdown-item" href="{{ path('inventaire_index') }}">Votre inventaire</a>
                    <a class="dropdown-item" href="{{ path('shop') }}">Magasin</a>
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" id="DropdownInfos" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Informations
                </a>
                <div class="dropdown-menu" aria-labelledby="DropdownInfos">
                    <a class="dropdown-item" href="{{ path('infos') }}">Informations sur le jeu</a>
                    <a class="dropdown-item" href="{{ path('data') }}">Vos informations</a>
                </div>
            </li>
        </ul>


        <ul class="nav navbar-nav navbar-right">
            <!-- Authentication Links -->
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <span class="navbar-text">
                    Bonjour {{ app.user.username }}
                    {% if is_granted('ROLE_ADMIN') %} (role : Admin)&nbsp &nbsp
                    {% elseif is_granted('ROLE_USER') %} (role : User) &nbsp &nbsp{% endif %}
            </span>
                <a class="btn btn-outline-success" type="button" href="{{path('app_logout') }}">
                    Se déconnecter
                </a>

            {% else %}
                <a class="btn btn-outline-success" type="button" href="{{path('app_login') }}">
                    Se connecter
                </a>
                <a class="btn btn-outline-success" type="button" href="{#{path('changePassword') }#}">
                    mot de passe oublié
                </a>
            {% endif %}

        </ul>
    </div>
</nav>
